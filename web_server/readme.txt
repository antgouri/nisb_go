main.go - Demonstrates a simple webserver program in golang. 

1. Run the code in a terminal with the command:

go run main.go

2. Then open a browser with the above command is still running

3. Type the url localhost:8000 

4. The localhost page will display the message "Hello World"

These steps if performed in the right manner - then it implies it is successful execution of the webserver code...
